<header class="c-header">
    <h1>
        <a href="{{site.url}}">
            Rocket
        </a>
    </h1>
    <ul>
        {% for item in menu.items %}
            <li><a href="{{item.link}}">{{item.title}}</a></li>
        {% endfor %}
    </ul>
</header>
